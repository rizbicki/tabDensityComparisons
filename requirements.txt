# Core scientific stack
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# Tabular foundation models
tabpfn>=0.1.0
tabicl>=0.2.0

# Tree-based baseline
xgboost>=2.0.0

# PyTorch (needed by TabPFN and TabICLv2)
# If you already have a CUDA-enabled torch, comment this out
# to avoid overwriting it with a CPU-only version.
# torch>=2.0.0
